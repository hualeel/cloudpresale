# SolveIQ — 项目总结报告

> 版本：v2.0 | 日期：2026-03-01 | Intelligent Presales Collaboration Platform

**在线体验**：[http://106.54.237.35](http://106.54.237.35) — 请联系管理员获取演示账号

---

## 一、项目背景

### 行业痛点

云原生解决方案的售前工作极度依赖专家经验，传统模式面临以下核心挑战：

| 痛点 | 具体表现 |
|------|----------|
| **方案生成慢** | 一份完整的云原生技术方案需要架构师、安全、容量规划等多个专家协作 3~7 天 |
| **质量参差不齐** | 方案质量高度依赖个人能力，缺乏标准化模板和最佳实践沉淀 |
| **知识孤岛** | 历史成功方案分散在个人电脑，无法有效复用和传承 |
| **迭代成本高** | 客户需求变更后，全套文档需人工重写，响应速度慢 |
| **合规要求严** | 金融行业（银行、保险、证券）有严格的等保、监管合规要求，方案必须精准覆盖 |

### 项目定位

**SolveIQ** 是一套面向云原生解决方案销售团队的 **AI 驱动售前方案生成与团队协同平台**。

通过多 Agent 并行协作、RAG 知识库检索和多模型 LLM 支持，平台能够在 **3~5 分钟内** 自动生成覆盖架构、容量、安全、迁移、实施计划、报价六大维度的完整技术方案，并自动输出 Word 技术文档和多套 PowerPoint 演示文稿，同时提供完整的团队协作与商机管理工作流。

### 目标用户

- **售前架构师**：快速生成初稿，专注高价值优化与客户沟通
- **销售团队**：自助生成方案摘要，大幅提升客户响应速度
- **方案经理**：统一管理客户、商机、需求、方案全生命周期
- **管理层**：掌握团队绩效、商机进展、转化率等关键数据

---

## 二、功能介绍

### 2.1 客户与商机管理

**客户管理（Customer）**
- 多维客户档案：行业分类、客户等级（战略/重点/普通）、描述信息
- 支持搜索、筛选、分页浏览
- 层级树状视图：一屏展示客户 → 商机 → 需求的完整层级关系

**商机管理（Opportunity）**
- 商机看板视图（Kanban）：线索 → 资格认定 → 方案制作 → 演示 → 谈判 → 成交
- 商机金额追踪、关键需求记录
- 支持多人协作，商机可分配给多名团队成员

### 2.2 需求管理

- 结构化需求录入：自由文本 + 结构化字段并存
- AI 需求解析：自动提取关键技术指标（规模、合规要求、现有系统等）
- 需求审批流：草稿 → 确认（触发 AI 生成）
- 版本追溯：需求变更历史完整记录

### 2.3 AI 方案生成（核心功能）

一键触发 6 个专业 AI Agent **并行**生成方案，每个 Agent 聚焦一个专业维度：

| Agent | 专业领域 | 输出内容 |
|-------|----------|----------|
| **arch** | 云原生技术架构设计 | 架构总览、核心组件选型、网络/存储架构、高可用设计 |
| **sizing** | 资源规划与容量设计 | 集群规划、节点规格明细、资源汇总表、扩容预留 |
| **security** | 安全合规与等保设计 | 等保合规映射、RBAC 权限、镜像安全、数据加密 |
| **migration** | 应用迁移与上云路径 | 迁移策略、应用分类评估、分阶段计划、风险缓解 |
| **plan** | 项目实施计划 | 里程碑计划、团队配置、交付标准、风险矩阵 |
| **pricing** | 报价与商务方案 | 硬件/软件/服务报价明细、ROI 分析、商务策略 |

**实时进度追踪**：前端轮询实时显示各 Agent 执行状态（待执行 / 执行中 / 完成 / 失败）

**RAG 知识库增强**：已审批方案自动入库 ChromaDB，新方案生成时检索相似历史案例，注入上下文提升质量

### 2.4 方案版本管理

- 语义化版本控制（1.0 → 1.1 → 2.0）
- 方案状态流转：生成中 → 草稿 → 审核中 → 已审批 → 归档
- 多版本并存，一键切换当前版本
- 版本变更说明（change note）完整记录

### 2.5 交付物生成与下载

自动生成 7 类专业文档：

| 交付物类型 | 格式 | 内容 |
|-----------|------|------|
| 技术方案书 | Word (.docx) | 完整技术方案，含封面、目录、六大章节 |
| 总体汇报 PPT | PowerPoint (.pptx) | 管理层汇报，架构总览 + 核心亮点 |
| 容器平台 PPT | PowerPoint (.pptx) | 容器平台专项方案 |
| DevOps PPT | PowerPoint (.pptx) | CI/CD 流水线专项方案 |
| 高管摘要 PPT | PowerPoint (.pptx) | 极简风格，聚焦商业价值 |
| 安全合规 PPT | PowerPoint (.pptx) | 等保合规专项，含合规检查清单 |
| 执行计划 PPT | PowerPoint (.pptx) | 项目分阶段实施路线图 |

- 文件存储于 MinIO 对象存储，预签名 URL 安全下载
- **ZIP 批量打包下载**：一键下载方案全套文档

### 2.6 知识展示页面

- **CI/CD 流水线**：可视化展示标准 DevOps 工作流（含演示数据）
- **K8s 架构图**：展示云原生平台参考架构（含演示数据）

### 2.7 团队管理

- 多角色体系：管理员（admin）/ 高级架构师（senior_sa）/ 架构师（sa）/ 初级架构师（junior_sa）
- 团队成员列表，含本月方案数、负责商机数等绩效数据
- 支持商机分配与多人协作

### 2.8 系统设置

- **LLM 配置**：支持 Claude（Anthropic）和 DeepSeek 双模型提供商，可动态切换，无需重启
- **系统参数**：公司名称、Logo、联系信息等品牌配置
- API Key 安全管理（数据库存储，不明文展示）
- 一键连通性测试，实时验证模型可用性

---

## 三、项目亮点

### 3.1 多 Agent 并行架构 — 效率提升 6 倍

```
客户需求输入
     │
     ▼
┌─────────────────────────────────────────┐
│        Orchestrator (编排器)             │
│  ThreadPoolExecutor — 6 线程并行执行     │
└──┬──────┬──────┬──────┬──────┬──────────┘
   │      │      │      │      │
  arch  sizing security migration plan  pricing
   │      │      │      │      │      │
   └──────┴──────┴──────┴──────┴──────┘
                    │
              内容汇总合并
                    │
            生成 Word / PPT
```

传统串行调用需要 6 × 单次 LLM 耗时，并行架构将总耗时压缩至单次量级（**约 60~90 秒**生成完整方案），相比人工 3~7 天的周期，**效率提升超过 100 倍**。每个 Agent 具备独立的专业提示词设计，确保各维度内容的专业深度。

### 3.2 RAG 知识库自动沉淀 — 越用越聪明

- 方案审批通过后自动提取文本，向量化存入 ChromaDB
- 生成新方案时，根据需求内容检索 Top-K 相似历史方案
- 历史成功经验自动注入 Prompt，持续提升方案质量
- 实现**组织知识资产化**：每次生成都在为团队积累可复用的方案经验

随着使用量增长，平台对同类客户（如金融行业等保合规、大型企业迁移上云）的方案质量将持续迭代优化，形成正向飞轮效应。

### 3.3 双 LLM 提供商支持 — 灵活适配不同场景

```
┌─────────────────────────────────────────┐
│          base_agent.py                  │
│                                         │
│  model.startswith("deepseek") ?         │
│   ├── YES → OpenAI SDK                  │
│   │         base_url=api.deepseek.com   │
│   └── NO  → Anthropic SDK              │
│             (claude-sonnet-4-6)         │
└─────────────────────────────────────────┘
```

- 支持 **Claude**（claude-sonnet-4-6）和 **DeepSeek**（deepseek-chat / deepseek-reasoner）
- 模型和 API Key 动态从数据库读取，管理员通过 Settings 页面随时切换，**零停机**
- Celery Worker 进程隔离，每次任务独立读取配置，无需重启服务
- 适配国内网络环境（DeepSeek）和高精度场景（Claude），灵活选择性价比最优方案

### 3.4 全生命周期方案版本管理

同一需求支持生成多个方案版本，完整覆盖方案演进全流程：

```
需求确认 → [生成中] → [草稿] → [审核中] → [已审批] → [归档]
                          ↑                    ↓
                      版本迭代              RAG 入库
                   (v1.0 → v1.1)        （知识沉淀）
```

- 语义版本号自动递增，版本变更说明完整留存
- 同一需求多版本并存，随时回溯历史版本
- 已审批方案自动沉淀至 RAG 知识库，形成组织资产

### 3.5 企业级安全与合规设计

| 安全维度 | 实现方式 |
|---------|---------|
| **身份认证** | JWT + bcrypt，Token 过期自动刷新 |
| **权限控制** | 4 级 RBAC（admin / senior_sa / sa / junior_sa），接口级鉴权 |
| **审计日志** | 全操作记录：操作人、时间、资源类型、变更内容 |
| **文件安全** | MinIO 预签名 URL，访问链接带过期时间，防止直链泄露 |
| **密钥管理** | API Key 数据库存储，不在日志/响应中明文暴露 |

### 3.6 七类专业交付物自动生成

一次 AI 生成，自动输出 **1 份 Word 技术方案 + 6 套差异化 PPT**，覆盖不同汇报场景：

- **技术方案书（Word）**：面向技术团队，完整六章结构，含封面目录
- **总体汇报 PPT**：面向决策层，架构亮点 + 业务价值
- **高管摘要 PPT**：极简风格，聚焦 ROI 和商业价值
- **容器平台专项 PPT**：容器化改造方案深度展示
- **DevOps 专项 PPT**：CI/CD 流水线与 DevOps 体系建设
- **安全合规专项 PPT**：等保映射 + 合规检查清单
- **实施计划 PPT**：分阶段里程碑路线图

支持 **ZIP 批量打包下载**，一键获取全套材料。

### 3.7 实时可视化进度追踪

AI 方案生成期间，前端实时展示各 Agent 执行状态：

```
[arch]       ████████████████████ 完成 ✓
[sizing]     ████████████░░░░░░░░ 执行中 65%
[security]   ████████████████████ 完成 ✓
[migration]  ░░░░░░░░░░░░░░░░░░░░ 等待中
[plan]       ████████████████████ 完成 ✓
[pricing]    ░░░░░░░░░░░░░░░░░░░░ 等待中
```

用户无需刷新页面，实时感知生成进度，大幅降低等待焦虑，提升使用体验。

### 3.8 统一团队协作工作台

- **总览仪表盘**：全团队商机转化漏斗、本月产出趋势、近期活动流
- **商机看板（Kanban）**：拖拽式阶段管理，直观掌握全局商机状态
- **层级树状视图**：客户 → 商机 → 需求 → 方案四级层级一屏呈现
- **团队成员管理**：本月方案数、负责商机数等个人绩效一目了然

所有数据实时同步，多人操作互不干扰，真正实现**售前团队的数字化协同作战**。

### 3.9 一键 Docker 全栈部署

全栈容器化，`docker compose up -d` 一行命令启动 7 个服务：

- **多平台镜像**：同时支持 linux/amd64（服务器）和 linux/arm64（Apple Silicon）
- **健康检查**：PostgreSQL、Redis、MinIO 全部配置 healthcheck，服务按依赖顺序启动
- **数据持久化**：4 个 Docker Volume 保障数据安全
- **零代码配置**：通过环境变量和 Settings 页面完成全部配置，无需修改代码

---

## 四、技术架构

### 4.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        浏览器 (Browser)                      │
│              React 19 + TypeScript + Vite 7                 │
│           Zustand 状态管理 | 自定义 CSS 设计系统              │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP/REST
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    FastAPI 后端 (Port 8080)                   │
│    路由层: /auth /customers /opportunities /requirements     │
│           /solutions /deliverables /team /settings          │
│    中间件: JWT 认证 | CORS | 审计日志                         │
└────────┬──────────────┬──────────────────┬───────────────────┘
         │              │                  │
    同步读写         异步任务            文件存储
         │              │                  │
         ▼              ▼                  ▼
┌──────────────┐ ┌────────────────┐ ┌───────────────┐
│  PostgreSQL  │ │ Celery Worker  │ │     MinIO     │
│   (数据库)   │ │  (AI 任务执行) │ │  (文档存储)   │
│              │ │                │ └───────────────┘
│  9 个数据表  │ │ ThreadPool     │
│  JSONB 内容  │ │ 6 Agent 并行   │ ┌───────────────┐
└──────────────┘ │                │ │   ChromaDB    │
                 │ Anthropic SDK  │ │  (向量检索)   │
┌──────────────┐ │ OpenAI SDK     │ │  RAG 知识库   │
│    Redis     │ └────────────────┘ └───────────────┘
│  任务队列    │
│  结果存储    │
└──────────────┘
```

### 4.2 后端技术栈

| 组件 | 技术选型 | 用途 |
|------|----------|------|
| Web 框架 | FastAPI 0.115 | RESTful API，自动 OpenAPI 文档 |
| ORM | SQLAlchemy 2.0 | 数据模型，支持关联查询 |
| 数据库 | PostgreSQL 16 | 主数据存储，JSONB 存方案内容 |
| 异步任务 | Celery 5.x + Redis | AI 生成任务异步处理 |
| 对象存储 | MinIO | Word/PPT 文件存储 |
| 向量数据库 | ChromaDB | RAG 相似度检索 |
| AI SDK | anthropic + openai | 双 LLM 提供商 |
| 文档生成 | python-docx + python-pptx | Word/PPT 自动生成 |
| 认证 | JWT (python-jose) + bcrypt | 用户认证与安全 |
| 迁移 | Alembic | 数据库版本管理 |

### 4.3 前端技术栈

| 组件 | 技术选型 | 用途 |
|------|----------|------|
| 框架 | React 19 + TypeScript | 前端主框架 |
| 构建工具 | Vite 7 | 开发服务器与生产构建 |
| 状态管理 | Zustand | 全局状态（用户、Token、页面） |
| HTTP 客户端 | Axios | API 请求，自动携带 JWT |
| 样式 | 自定义 CSS 设计系统 | 暗色主题，高密度信息展示 |
| 服务部署 | Nginx | 静态文件服务 + API 反向代理 |

### 4.4 数据模型关系

```
User (用户)
  │
  ├── Customer (客户)
  │     └── Opportunity (商机) ──── many-to-many ──── User (分配成员)
  │           └── Requirement (需求)
  │                 └── Solution (方案，版本化)
  │                       ├── Deliverable (交付物，Word/PPT)
  │                       └── GenJob (生成任务进度记录)
  │
  └── AuditLog (审计日志)

SystemSetting (系统配置，KV 存储)
```

### 4.5 AI 生成流程

```
1. POST /solutions  →  创建 Solution 记录（status: generating）
2. Celery Task 异步触发
3. Orchestrator 读取需求内容
4. ChromaDB 检索相似历史案例（RAG）
5. ThreadPoolExecutor 并发启动 6 个 Agent
6. 每个 Agent：
   a. 构建 Prompt（需求 + RAG 上下文）
   b. 调用 LLM API（流式输出）
   c. 更新 GenJob 进度（0% → 100%）
7. 所有 Agent 完成后，合并结果 → Solution.content（JSONB）
8. 触发文档生成（Word + 6 套 PPT）
9. 文件上传 MinIO，写入预签名下载 URL
10. Solution.status → "draft"，通知前端刷新
```

---

## 五、部署架构

### 5.1 容器编排架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Docker Compose 部署                       │
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────┐ │
│  │  frontend   │    │     api     │    │     worker      │ │
│  │  nginx:80   │───▶│  uvicorn    │    │  celery worker  │ │
│  │  React SPA  │    │  :8080      │    │  concurrency=4  │ │
│  └─────────────┘    └──────┬──────┘    └────────┬────────┘ │
│                            │                    │           │
│         ┌──────────────────┼────────────────────┘           │
│         │                  │                                │
│  ┌──────▼──────┐   ┌───────▼─────┐   ┌────────────────┐   │
│  │  postgres   │   │    redis    │   │     minio      │   │
│  │  :5432      │   │    :6379    │   │  :9000 / :9001 │   │
│  │  数据库     │   │  任务队列   │   │  对象存储      │   │
│  └─────────────┘   └─────────────┘   └────────────────┘   │
│                                                             │
│  ┌─────────────┐                                           │
│  │   chroma    │                                           │
│  │   :8000     │                                           │
│  │  向量数据库  │                                           │
│  └─────────────┘                                           │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 网络端口映射

| 容器 | 宿主机端口 | 说明 |
|------|-----------|------|
| frontend | 80 | Web 入口（Nginx） |
| api | 8080 | REST API + OpenAPI 文档 |
| postgres | 5432 | 数据库 |
| redis | 6379 | 消息队列 |
| minio | 9000 | 对象存储 API |
| minio | 9001 | MinIO 控制台 |
| chroma | 8000 | 向量数据库 |

### 5.3 数据持久化

| 数据卷 | 存储内容 |
|--------|---------|
| postgres_data | 全量业务数据（客户/商机/方案/用户） |
| redis_data | 任务队列与缓存 |
| minio_data | 生成的 Word / PPT 文件 |
| chroma_data | RAG 向量索引 |

### 5.4 服务依赖与启动顺序

```
postgres (healthy) ─┐
redis    (healthy) ─┼─▶ api ─▶ frontend
minio    (healthy) ─┘
redis    (healthy) ─┐
postgres (healthy) ─┴─▶ worker
```

---

## 六、公有云访问

当前系统已部署于腾讯云公有云服务器，支持公网直接访问。

### 6.1 访问地址

| 入口 | 地址 | 说明 |
|------|------|------|
| **Web 系统主入口** | `http://106.54.237.35` | 登录后使用全部功能 |

### 6.2 演示账号

系统内置多个演示账号，涵盖不同角色权限：

| 角色 | 权限范围 |
|------|---------|
| 系统管理员 | 全部功能，含 LLM 配置、用户管理、系统设置 |
| 高级架构师 | 方案生成、客户/商机/需求管理、交付物下载 |
| 架构师 | 方案生成、负责商机的需求与方案操作 |

> 请联系管理员获取演示账号及密码。

### 6.3 演示数据说明

系统已预置真实感演示数据，覆盖金融行业典型售前场景：

- **6 家客户**：工商银行、招商银行、上海银行、中国人寿、中信证券、华夏基金
- **9 个商机**：覆盖线索→成交全阶段，含不同金额量级
- **9 条需求**：各字段全量填写，包含合规要求、规模指标等
- **7 份方案**：含已审批、审核中、草稿等各状态
- **42 份交付物**：Word 技术方案 + 多套 PPT，含已就绪和生成中状态

---

## 附录：项目代码结构

```
├── docker-compose.yml              # 部署编排（7 个服务）
├── backend/                        # FastAPI 后端
│   ├── app/
│   │   ├── main.py                 # FastAPI 入口
│   │   ├── api/                    # 路由层（10 个模块）
│   │   ├── models/                 # SQLAlchemy 数据模型（9 个）
│   │   ├── schemas/                # Pydantic 请求/响应模型
│   │   ├── agents/                 # AI Agent 系统
│   │   │   ├── base_agent.py       # 双 LLM 提供商执行器
│   │   │   ├── orchestrator.py     # 并行编排器
│   │   │   └── prompts.py          # 6 个 Agent 提示词
│   │   ├── document_gen/           # 文档生成
│   │   │   ├── word_generator.py   # Word 技术方案
│   │   │   ├── ppt_generator.py    # PPT 总体方案
│   │   │   ├── ppt_container.py    # PPT 容器平台
│   │   │   ├── ppt_devops.py       # PPT DevOps
│   │   │   └── ppt_security.py     # PPT 安全合规
│   │   └── services/
│   │       ├── celery_app.py       # Celery 任务定义
│   │       └── rag_service.py      # ChromaDB RAG 服务
│   └── scripts/
│       ├── init_db.py              # 数据库初始化
│       └── seed_demo.py            # 演示数据注入
└── cloudpresale/                   # React 前端（SolveIQ）
    ├── src/
    │   ├── pages/                  # 11 个页面组件
    │   ├── api/                    # API 客户端（types/client/index）
    │   ├── store/                  # Zustand 状态管理
    │   └── App.tsx                 # 路由配置
    └── Dockerfile                  # 前端容器（Nginx 多阶段构建）
```

---

*SolveIQ — From Requirements to Solutions, Powered by AI*
